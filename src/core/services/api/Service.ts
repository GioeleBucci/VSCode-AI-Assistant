/**
 * Tag interface for all services registered in the DI container
 * This ensures type safety while allowing any service to be registered
 */
export interface Service {
    // All services must implement this to be registered in the container
}
